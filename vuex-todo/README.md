# vuex-todo

To start the REST server:

- open a terminal window
- `cd server`
- `npm install`
- `npm start`

To start the web server:

- open a terminal window
- `npm install`
- `npm run serve`
- browse localhost:8080
